import styled from "styled-components";

export const StyledMain = styled.main`
    h2 {
        font-size: 32px;
        font-family: var(--font-default);
        font-weight: 500;
        color: var(--purple0); 
        text-align: center;
    }

    
`